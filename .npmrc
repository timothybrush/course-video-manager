# Keep a flat, npm-style node_modules layout. The app and the
# subtitle-overlay-renderer (Remotion) were built against hoisted resolution;
# hoisted linking avoids strict-peer resolution surprises during the migration.
node-linker=hoisted

# Keep the pnpm content-addressable store inside the repo (gitignored).
store-dir=.pnpm-store
